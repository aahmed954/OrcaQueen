# PostgreSQL pg_hba.conf for Replication
# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Local connections
local   all             all                                     trust

# Unix socket connections
host    all             all             127.0.0.1/32            md5

# Replication access from Railway (adjust IP/subnet as needed; 0.0.0.0/0 for testing)
host    replication     replicator      0.0.0.0/0               md5

# IPv4 remote connections from internal network
host    litellm         litellm         172.20.0.0/16           md5
host    litellm         litellm         0.0.0.0/0               md5  # For Railway access

# Reject all other connections
host    all             all             all                     reject